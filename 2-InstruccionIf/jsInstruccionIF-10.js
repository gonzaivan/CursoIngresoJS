function mostrar()
{
	let notas;

	notas = Math.floor(Math.random() * 11);

	if(notas > 8){
		alert("EXCELENTE");
	}
	else {
		if(notas > 3 && nota < 9){
			alert("APROBÓ");
		}
		else {
			alert("Vamos, la proxima se puede");
		}
	}
}

//FIN DE LA FUNCIÓN